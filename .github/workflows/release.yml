name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 1.0.0)'
        required: true
        type: string
      zig_version:
        description: 'Minimum Zig version (e.g., 0.15)'
        required: true
        default: '0.15'
        type: string
      prerelease:
        description: 'Mark as pre-release'
        required: false
        default: false
        type: boolean

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate version format
        run: |
          VERSION="${{ inputs.version }}"
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Error: Version must be in format X.Y.Z (e.g., 1.0.0)"
            exit 1
          fi
          echo "Version $VERSION is valid"

      - name: Check tag doesn't exist
        run: |
          TAG="v${{ inputs.version }}-zig${{ inputs.zig_version }}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Error: Tag $TAG already exists"
            exit 1
          fi
          echo "Tag $TAG is available"

  test:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: master

      - name: Run tests
        run: zig test mod.zig -lc

      - name: Build
        run: zig build

  release:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: master

      - name: Update version in build.zig.zon
        run: |
          sed -i 's/\.version = "[^"]*"/.version = "${{ inputs.version }}"/' build.zig.zon
          cat build.zig.zon | grep version

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit version bump
        run: |
          git add build.zig.zon
          git commit -m "Release v${{ inputs.version }}-zig${{ inputs.zig_version }}"

      - name: Create tag
        run: |
          TAG="v${{ inputs.version }}-zig${{ inputs.zig_version }}"
          git tag -a "$TAG" -m "Release $TAG"

      - name: Push changes
        run: |
          git push origin main
          git push origin "v${{ inputs.version }}-zig${{ inputs.zig_version }}"

      - name: Generate release notes
        id: notes
        run: |
          cat << 'EOF' > release_notes.md
          ## Blitz v${{ inputs.version }}

          High-performance parallel runtime for Zig.

          ### Requirements
          - **Zig**: ${{ inputs.zig_version }}.0 or later
          - **OS**: Linux, macOS, Windows

          ### Installation

          Add to your `build.zig.zon`:

          ```zig
          .dependencies = .{
              .blitz = .{
                  .url = "https://github.com/${{ github.repository }}/archive/v${{ inputs.version }}-zig${{ inputs.zig_version }}.tar.gz",
                  .hash = "...", // Run zig build to get the hash
              },
          },
          ```

          Then in `build.zig`:

          ```zig
          const blitz = b.dependency("blitz", .{
              .target = target,
              .optimize = optimize,
          });
          exe.root_module.addImport("blitz", blitz.module("blitz"));
          ```

          ### What's New

          See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.

          ---
          **Full Changelog**: https://github.com/${{ github.repository }}/compare/...v${{ inputs.version }}-zig${{ inputs.zig_version }}
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version }}-zig${{ inputs.zig_version }}
          name: Blitz v${{ inputs.version }} (Zig ${{ inputs.zig_version }}+)
          body_path: release_notes.md
          prerelease: ${{ inputs.prerelease }}
          generate_release_notes: true
